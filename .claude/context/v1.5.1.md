# v1.5.1 版本上下文

日期：2026-03-15
版本：v1.5.1

---

## 本版摘要

新增行程分類「紫星行程聊聊」，顏色為紫色。

---

## 主要變動

### 新增行程類型：紫星行程聊聊

| 項目 | 內容 |
|------|------|
| 類型名稱 | 紫星行程聊聊 |
| 前端標籤顏色 | 背景 `#F3E5F5`、文字 `#7B1FA2`（紫色） |
| 月曆點顏色 | `#7B1FA2` |
| Google Calendar 顏色 | colorId `3`（Grape 葡萄紫） |
| 整日預設 | 否（與活動、諮詢簽約相同，可自由切換） |
| 標題提示詞 | `時間+名字+聊聊or其他` |

### 修改檔案

| 檔案 | 修改內容 |
|------|----------|
| `public/add-event.html` | 新增 `<option value="紫星行程聊聊">` |
| `public/event-detail.html` | 編輯表單新增相同選項 |
| `public/list.html` | 新增 tab 按鈕與分頁內容區塊 |
| `public/js/add-event.js` | `TITLE_HINTS` 加入提示詞 |
| `public/js/event-detail.js` | `TITLE_HINTS` 加入提示詞 |
| `public/js/calendar.js` | 月曆點顏色 + 類型排序加入 |
| `public/js/list.js` | `eventsByType`、`tabTypes`、pageshow 重置加入 |
| `public/css/style.css` | `.event-type.紫星行程聊聊` 紫色標籤樣式 |
| `server/routes/calendar.js` | 兩處 `validTypes` 白名單加入 |
| `server/services/calendarService.js` | `EVENT_TYPE_COLOR_MAP` 加入 colorId `3` |

---

## 架構現況（v1.5.1）

- **前端**：純 HTML/JS/CSS（`public/`），React 版已廢棄
- **後端**：Node.js + Express.js（`server/`）
- **身份驗證**：LINE Login OAuth，不依賴 LIFF SDK
- **資料庫**：Supabase (PostgreSQL) + Google Calendar + Google Sheets
- **部署**：Vercel（前後端統一）
- **本機開發**：ngrok（內網穿透）

## 行程分類（v1.5.1 起）

| 類型 | 前端顏色 | Google Calendar | 整日預設 |
|------|---------|-----------------|---------|
| 學員上課 | 橙黃色 | 5 Banana | 是（鎖住） |
| 活動 | 紅色 | 11 Tomato | 否 |
| 諮詢簽約 | 綠色 | 10 Basil | 否 |
| 紫星行程聊聊 | 紫色 | 3 Grape | 否 |
