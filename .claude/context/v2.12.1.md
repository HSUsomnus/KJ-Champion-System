# v2.12.1 — Change 24 收尾清理

## 背景

change 22（規則體系重構，v2.11.0）與 change 23（模型分層工作證，v2.12.0）上線後，由顧問進行總驗收發現 4 個記錄檔掃尾項，均屬基礎設施維護，無產品程式碼變更。同時驗證三層流程（`/規劃` → `/實作` → 收尾員子代理）的初次實地運作。

## 改動檔案

- `.claude/CHANGELOG.md`：v2.8.1 完整摘要壓縮為一行索引（實踐「近 5 版保留完整摘要、更早版本壓縮為索引」規則，新增 v2.12.1 後近 5 版為 v2.12.1/v2.12.0/v2.11.0/v2.10.1/v2.10.0，v2.9.0 向下壓縮）
- `.claude/now.md`：移除 change 19/22/23 已標記 DONE 的行、更新 change 20/21 進行中說明（分支已改名為 `m_b_調查表單`、spec 已遷入 `changes/`）、驗證結果寫入
- `changes/22-規則體系重構/`：刪除整個資料夾（歷史由 v2.11.0 context + git log 承載）
- `changes/23-模型分層工作證/`：刪除整個資料夾（歷史由 v2.12.0 context + git log 承載）

## 關鍵設計決策

無。本版純記錄性維護。

## 學習日誌

無。

## 驗證結果

**Phase 1 — CHANGELOG 裁切**
- `grep -c "^git tag:" CHANGELOG.md`：6 → 5（v2.12.1 新增，v2.9.0 壓縮為索引）✅
- v2.8.1 由 16 行完整摘要縮為一行索引 ✅

**Phase 2 — 當前 Change 清單**
- `grep -c "^change" now.md`：3（change 12、20、24） ✅
- change 12「統一彈出訊息系統」0/33 task ✅
- change 20「團隊調查表單系統」分支 `m_b_調查表單` ✅
- change 24（本版） ✅
- 已 DONE：change 19/22/23 全部移除 ✅

**Phase 3 — 已完成 Changes 資料夾清理**
- `ls -la changes/ | wc -l`：僅 24-收尾清理 存在（22、23 已刪除） ✅
- 22-規則體系重構：不存在 ✅
- 23-模型分層工作證：不存在 ✅

**Phase 4 — 收尾員完成後驗收**
- `grep -c "^git tag:" CHANGELOG.md`：維持 5（收尾員自行裁切了 v2.9.0） ✅
- `.claude/context/v2.12.1.md`：已建立 ✅

## 後續注意

無。所有計畫項目已結項。
