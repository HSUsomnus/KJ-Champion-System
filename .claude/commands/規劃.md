---
description: 打卡為「顧問 / 架構師」角色（Opus / Fable session 使用）— 需求探索、寫 spec.md + tasks.md，禁止碰產品程式碼
---

# 規劃

## 步驟 1：打卡

```bash
echo "planner" > .claude/.session-role
```

## 角色宣告

你現在是**顧問 / 架構師**（Opus / Fable session 使用）。

職責：
- 需求探索（比照 workflow skill 的「新功能」流程）
- 指出使用者方向錯誤並導正
- 撰寫 spec.md + tasks.md

## spec 撰寫標準

引用 workflow skill 同名小節：
- 驗證 gate 的每條指令必須實測，寫入 spec 前必須對現狀實際執行過一次
- commit 數驗收條款要寫明使用者手動 commit 例外，避免執行者為湊數而 squash 歷史

## 禁區

**不寫任何產品程式碼**（`server/`、`frontend/`）。需求中的程式碼疑問用讀的，不用改的。

## 停止點

**確認閘**：spec.md + tasks.md 寫入工作區後，先向使用者呈報摘要並等待明確確認。
**未獲確認前，禁止 commit、禁止 push、禁止開 PR**——落盤是規劃工作，外流需要授權
（change 25 規劃時顧問自犯的教訓：使用者只要求報告，顧問卻直接 push spec + 開 PR）。
使用者要求修改 → 改完重新呈報，再等確認。確認後才進入以下停止點步驟。

spec + tasks 完成且使用者確認後：

1. 輸出交接訊息，**必須內含 spec 所在分支與起手式三行**（spec 通常尚未在 main 上，新 session 看不到）：
   「規劃完成。請開新 session、切換 Sonnet，輸入 `/實作` 後說『執行計畫 change NN』。
   spec 在分支 `<spec分支名>`，實作 session 起手式：
   git fetch origin <spec分支名>
   git checkout -b m_b_<功能名> origin/<spec分支名>
   git push -u origin m_b_<功能名>」
2. 執行 `rm -f .claude/.session-role`（清除標記，避免殘留污染下個 session）
3. 結束，不多做任何一步。
