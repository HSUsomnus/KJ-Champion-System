# Change 23 — 收尾筆記

## 版本號

v2.12.0

## 改動摘要（給 CHANGELOG 用）

新增「模型分層工作證」三層角色邊界系統：
- `.claude/commands/規劃.md`、`實作.md`：使用者打卡角色 command（Opus/Fable 規劃、Sonnet 實作）
- `.claude/agents/收尾員.md`：功能上線收尾子代理（Haiku），負責 CHANGELOG / context / now.md / tag 指令
- `.claude/hooks/role-guard.js`：讀 `.claude/.session-role` 標記，deny 攔截 planner 寫產品碼、engineer 執行 `git tag` / push main；無標記完全不影響既有行為
- `.claude/hooks/lang-reminder.js`：`UserPromptSubmit` 每回合注入繁體中文回報提醒（語言紀律三層治理第 2 層）
- CLAUDE.md 新增「Session 角色」小節 + 語言鐵律一行；workflow / deploy-release skill 補述角色建議與收尾員說明、commit trailer 慣例
- `.gitignore` 加入 `.claude/.session-role`

純 `.claude/` 基礎設施變更，不含任何 `server/`、`frontend/` 產品程式碼。

## 學習日誌素材

### 問題 1：新指令檔案沒被 git 追蹤到

**遇到的問題**：Phase 1 建立 `.claude/commands/規劃.md` 與 `實作.md` 後，`git status` 完全沒顯示這兩個新檔案，即使 `.gitignore` 已有 `!.claude/commands/*.md` 例外規則。
**試過的方法**：先確認檔案確實存在、確認例外規則寫法看起來正確——都沒問題，於是用 `git check-ignore -v` 直接檢查才發現規則被 `.gitignore:28:.claude/commands/` 這行擋住。
**最終解法**：把 `.gitignore` 的 `.claude/commands/`（目錄整體排除）改成 `.claude/commands/*`（排除目錄內容，不排除目錄本身），例外規則才生效。
**根本原因**：git 的規則是「父目錄本身被排除時，無法用後面的例外規則重新納入其中的檔案」——舊寫法排除的是目錄本身，`!.claude/commands/*.md` 對新檔案不生效；既有的 `子代理.md` 能被追蹤到，是因為它是規則加入前就已經 commit 過的既有追蹤檔案，不受此限制。

### 問題 2：其餘

無——role-guard.js / lang-reminder.js 首次撰寫、`node --check` 與 spec 第七節模擬情境（無標記放行 / planner 越界 deny / engineer git tag deny / heredoc 不誤判 / git-guard regression）全部一次通過，未遇到額外的坑。

## 驗證結果摘要

- gate 7.1–7.4：全數執行一次 ✅，Phase 4 完成後重跑 7.1–7.4 做 regression 檢查，結果不變 ✅
- 常駐 token 估算：3148（門檻 3500）
- `git log --oneline main..HEAD`：5 個 commit（Phase 1–4 各一個 + 收尾 1 個），全帶型別前綴 + `Co-Authored-By`/`Claude-Session` trailer
- tasks.md 全部 task 已勾選，勾選與對應 Phase commit 同批
