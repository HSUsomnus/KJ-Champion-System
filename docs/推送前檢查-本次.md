# 本次推送前檢查（機密與項目清單）

> 請依此清單確認後再推送。推送後請刪除此檔或改為「已推送」註記，避免重複使用。

---

## 一、已完成的準備

- [x] **.gitignore** 已包含：`.env`、`.env.backup`、`.env.local`、`Key/`、`*.json`（除 package）
- [x] **程式碼**僅使用 `process.env.XXX`，無硬編碼 API Key、Secret、Token、私鑰
- [x] **.env / .env.backup** 未出現在 `git status`（不會被 commit）

---

## 二、本次將推送的項目（請你核對）

### 修改過的檔案（M）

| 檔案 | 說明 |
|------|------|
| `.gitignore` | 新增 `.env.backup`，避免誤推備份檔 |
| `docs/成員資料試算表欄位說明.md` | 進階資訊欄位 H、I、J 說明 |
| `public/js/member-detail.js` | 成員詳情顯示進階資訊、課程志工解析 |
| `public/js/profile.js` | 個人資料進階資訊、課程志工新增/儲存 |
| `public/member-detail.html` | 成員詳情進階資訊區塊 |
| `public/profile.html` | 個人資料進階資訊區塊、註冊表進階選填 |
| `server/routes/profile.js` | 註冊/更新接收 teslaFranchisee、teamResponsibilities、volunteerRecords |
| `server/services/sheetService.js` | 讀寫 A~J 欄（進階資訊 H/I/J） |

### 新增的檔案／資料夾（??）

| 項目 | 說明 |
|------|------|
| `api/` | Vercel Serverless 入口（`[[...path]].js` 僅 require server） |
| `cleanup-unpushed.ps1` | 本機清理未推送腳本 |
| `docs/前端版本與試算表指南.md` | 文件 |
| `docs/如何更新成新前端.md` | 文件 |
| `docs/現有前端版本說明.md` | 文件 |
| `docs/詳細部署到Vercel.md` | 文件 |
| `docs/權限設定修正指引.md` | 文件 |
| `frontend/` | React 前端（Vite） |
| `專案更新版本/` | 本機歸檔用資料夾 |
| `現有專案版本/` | 本機歸檔用資料夾 |

---

## 三、機密檢查結果（第一次）

- **.env、.env.backup**：已列入 .gitignore，`git status` 中**未出現** ✅  
- **Key/**：已列入 .gitignore ✅  
- **程式碼**：僅使用 `process.env.XXX`，無寫死金鑰 ✅  
- **文件**：僅出現範例值（如 `your-channel-secret`、`-----BEGIN PRIVATE KEY-----\n...`），無你的實際金鑰 ✅  

**結論：目前清單中無機密檔案，可推送的皆為程式碼與文件。**

---

## 四、你可再自行確認

1. 在專案根目錄執行：`git status`  
   - 確認清單中**沒有** `.env`、`.env.backup`、`Key/`、任何金鑰檔。
2. 若曾執行過 `git add .env` 或 `git add .env.backup`：  
   - 執行 `git reset HEAD .env .env.backup` 後再檢查。

---

## 五、推送步驟（你確認沒問題後）

1. **停止本機伺服器與 ngrok**（在對應終端機按 Ctrl+C）。
2. **第二次機密掃描**（見下方「再次掃描」）。
3. 你再次確認沒問題後，執行：
   - `git add .`（或只 add 你要的檔案；.gitignore 會排除 .env、.env.backup、Key/）
   - `git commit -m "feat: 個人資料進階資訊、Google Sheet H/I/J 欄、推送前機密檢查"`
   - `git push origin main`（或你的預設分支）
4. 若已設定 Cloud Run / Vercel 自動部署，推送後會自動觸發部署。

---

## 六、再次掃描（推送前最後一次）— 已完成 ✅

已對**整個專案**（含將被 add 的檔案）進行第二次機密掃描，結果如下：

| 檢查項目 | 結果 |
|----------|------|
| 長金鑰／Token 寫死在程式或文件（KEY/SECRET/TOKEN= 後接 20+ 字元） | 僅文件中的**範例**（如 `your-channel-secret`、`abcdef...`、`1234567890-AbcdEfgh`），無實際金鑰 ✅ |
| 完整私鑰內容（-----BEGIN PRIVATE KEY----- 後接長 base64） | 未在任何會被 commit 的檔案中出現 ✅ |
| 真實 Calendar ID / Service Account Email（@group.calendar.google.com、@*.iam.gserviceaccount.com） | 未在程式碼中出現；僅 DEPLOYMENT.md 有占位符 `your-project-id`、`your-group-calendar-id` ✅ |
| 程式碼是否印出 process.env 或金鑰（console.log(process.env...)） | 未發現 ✅ |
| .env、.env.backup、Key/ 是否被 .gitignore 排除 | 已排除（.gitignore 第 1～5 行、第 44 行）✅ |

**結論：整個專案（含將被 add 的檔案）未發現機密外洩，可安全推送並觸發自動部署。**

完成兩次確認後即可執行推送（見下方指令）。
