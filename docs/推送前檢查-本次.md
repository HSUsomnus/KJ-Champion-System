# 推送前檢查（本次）

推送到 GitHub 前請依此清單確認機密資料不會被送出。

---

## 一、機密檢查結果（已掃描）

| 檢查項目 | 結果 |
|----------|------|
| `.env` 是否在 .gitignore | ✅ 已列入（第 2 行） |
| `.env` 是否被 git 追蹤 | ✅ 未追蹤（不會被 push） |
| 程式碼中是否用 `process.env.XXX` 讀取機密 | ✅ 是，無硬編碼金鑰 |
| 實際 Channel Secret / 私鑰是否出現在「會被 commit」的檔案 | ✅ 未出現在 .js / .md / .html（僅在 .env，.env 不推送） |
| `Key/`、`*.json`（金鑰檔） | ✅ .gitignore 已排除 |
| 本次 `git status` 是否出現 .env / Key/ | ✅ 未出現（僅一般程式與文件變更） |

---

## 二、絕對不能進 Git 的檔案（再次確認）

- **`.env`**：內含 GOOGLE_PRIVATE_KEY、LINE_CHANNEL_SECRET、LINE_CHANNEL_ACCESS_TOKEN 等，**僅能存在本機**。
- **`Key/`**：Google 服務帳戶 JSON。
- 任何 **`*.json`**（除 `package.json`、`package-lock.json`）。

推上前請執行：

```powershell
git status
```

清單中**不得**出現：`.env`、`Key/`、或金鑰類 `.json`。

---

## 三、若曾誤 add 過 .env 或 Key/

```powershell
git reset HEAD .env
git reset HEAD Key/
git reset HEAD *.json
# 再檢查
git status
```

確認上述檔案不在「將被 commit」的列表中。

---

## 四、安全推送步驟

1. **確認機密未入版控**
   ```powershell
   cd "c:\Users\hu199\OneDrive\桌面\My Project\Line_Liff"
   git status
   ```
   確認沒有 .env、Key/、金鑰 .json。

2. **加入變更並提交**
   ```powershell
   git add -A
   git status
   ```
   再次確認清單中沒有機密檔案，再 commit：
   ```powershell
   git commit -m "feat: 整日鎖定/頭像同步/長字串換行/標題提示優化等"
   ```

3. **推送到 GitHub**
   ```powershell
   git push origin main
   ```

---

## 五、程式碼與文件中的「機密」說明

- **文件**：僅出現範例值（如 `your-channel-secret`、`-----BEGIN PRIVATE KEY-----\n...`），無你的實際金鑰 ✅  
- **程式**：金鑰皆透過 `process.env.XXX` 讀取，無寫死 ✅  
- **模擬用 ID**：`U11111111111111111111111111111111` 為開發模式用假 ID，可提交 ✅  

---

**總結：推上 GitHub 的只有程式碼與文件；金鑰與 ID 只放在本機 `.env` 或 Cloud Run 環境變數，不進 Git。**
