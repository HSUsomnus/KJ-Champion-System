# 推送前檢查（本次）

## 一、機密檢查

| 項目 | 結果 |
|------|------|
| `.env` 是否在 .gitignore | ✅ 已列入 |
| 本次變更是否包含 .env / Key/ | ✅ 未包含（僅 public/js/financial-upload.js） |
| 程式碼是否硬編碼金鑰／私鑰／Token | ✅ 否，僅讀取 process.env 或 API |
| 變更內容是否含實際 LIFF ID、Channel Secret、私鑰字串 | ✅ 否 |

## 二、本次變更摘要

- **點「瀏覽」改為開啟 Google 試算表連結**（有設定試算表時）；無設定時仍開 APP 內預覽
- 檔案：`public/js/financial-upload.js`（新增 `sheetViewUrl`、`handleBrowseClick`，載入時先取試算表連結）

## 三、確認清單

推送前請確認 `git status` 清單中**不得**出現：`.env`、`Key/`、或金鑰類 `.json`。
